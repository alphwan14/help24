<?xml version="1.0" encoding="utf-8"?>
<!-- Identical to values-v31: the splash is deliberately brand-dark in both
     modes (like Uber/Bolt) so it always matches the app's dark-first theme.
     This file must exist — night qualifiers outrank version qualifiers, so
     without it a dark-mode device would fall back to values-night (no splash
     icon config) instead of values-v31. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_v31</item>
        <item name="android:windowSplashScreenAnimationDuration">250</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
