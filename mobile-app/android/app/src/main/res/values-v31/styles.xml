<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ splash (SplashScreen API). Without this, the system falls back
     to the raw launcher icon, which renders the full-bleed logo tile oversized.
     splash_icon_v31 is a 288dp asset whose white disc exactly fills the system's
     2/3 circular mask, with the logo at professional proportions inside it. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_v31</item>
        <item name="android:windowSplashScreenAnimationDuration">250</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
