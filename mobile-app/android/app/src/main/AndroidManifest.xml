<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for network access -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- local_auth: biometric / device PIN for secure M-Pesa number change -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <!-- Location for messaging -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!--
        Journey foreground service (Phase 2). A journey must keep sharing while
        the screen is locked or the app is backgrounded, so geolocator runs its
        GeolocatorLocationService (declared by the plugin with
        foregroundServiceType="location") with an ongoing notification.

        Deliberately NOT requesting ACCESS_BACKGROUND_LOCATION: a location-typed
        foreground service started while the app is visible keeps receiving
        updates on while-in-use permission alone. Background location would be a
        far broader grant, a Play Console policy declaration, and a scarier
        install-time prompt — for no capability this feature needs.

        The service is started only by an explicit "Start journey" and stops the
        moment the journey ends (arrive / stop / cap), because it lives and dies
        with the position-stream subscription.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="Help24"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!--
            FCM default channel — CRITICAL for sound on Android 8+.
            Without this, FCM uses the system "miscellaneous" channel which
            is IMPORTANCE_DEFAULT (no sound). This must match:
              MainActivity.kt  → NotificationChannel id
              notification_service.dart → _kChannelId
              backend → android.notification.channelId
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="help24_high_importance" />
        <!-- Google Maps: set com.google.android.geo.API_KEY to your key (e.g. in build.gradle from local.properties) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}"/>
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
